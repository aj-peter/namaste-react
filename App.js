import React from "react";
import ReactDOM from "react-dom/client";

const JsxHeading = <h1 id="heading">Hello World by React 🚀</h1>;

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(JsxHeading);
